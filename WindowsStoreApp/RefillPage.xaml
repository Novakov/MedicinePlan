<local:BasePage
    x:Class="WindowsStoreApp.RefillPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsStoreApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"    
    xmlns:vm="using:WindowsStoreApp.ViewModels"
    xmlns:callisto="using:Callisto.Controls"
    d:DataContext="{d:DesignInstance Type=vm:RefillViewModel}"
    mc:Ignorable="d">
    <StackPanel 
        Margin="116,0,0,46"
        Width="300"
        HorizontalAlignment="Left"
        Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
        >      
        <TextBlock 
            Margin="0,15,0,0"
            FontSize="15">Date</TextBlock>
        <DatePicker
            Margin="0,15,0,0"
            Date="{Binding Date, Mode=TwoWay}"
            />        

        <TextBlock 
            Margin="0,15,0,0"
            FontSize="15">Count:</TextBlock>
        <callisto:NumericUpDown
            Margin="0,15,0,0"
            Minimum="1"
            Maximum="9999999"
            Increment="5"
            DecimalPlaces="0"
            Value="{Binding Count, Mode=TwoWay}"
            />

        <Button
            Margin="0,15,0,0"
            HorizontalAlignment="Right"
            Content="Refill"
            Command="{Binding RefillCommand}"
            />
    </StackPanel>
</local:BasePage>
